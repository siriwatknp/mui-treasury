import { PreviewComponent } from "@/components/preview-page";
import LoginForm from "@/registry/components/login-form/login-form";

export default function Page() {
  return (
    <PreviewComponent>
      <LoginForm />
    </PreviewComponent>
  );
}