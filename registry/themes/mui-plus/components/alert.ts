import { ThemeComponents } from "../types";

export const alertTheme: ThemeComponents = {
  MuiAlert: {
    styleOverrides: {
      root: {
        "--Icon-color": "currentColor",
      },
    },
  },
};
