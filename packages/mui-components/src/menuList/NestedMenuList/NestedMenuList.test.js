import React from 'react';
import { render } from '@testing-library/react';

describe('NestedMenuList', () => {
  // todo: 1. test that amount of item is correct at first paint
  test('First paint: has correct amount of list items', () => {});
  // todo: 2 activate one parent list must have more list items
  // todo: 2.1 deactivate that parent must have list equal to state before
  // todo: 3 activate one nested-parent list must have more list items
  // todo: 3.2 deactivate that nested-parent must have list equal to state before
});
