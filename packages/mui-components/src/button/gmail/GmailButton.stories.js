import React from 'react';
import { storiesOf } from '@storybook/react';
import GmailButton from './GmailButton';

storiesOf('mui-components|Button', module).add('gmail', () => (
  <GmailButton />
));
