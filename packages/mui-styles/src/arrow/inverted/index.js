import makeStyles from '@material-ui/core/styles/makeStyles';
import invertedArrowStyles from './invertedArrow.styles';

const useInvertedArrowStyles = makeStyles(invertedArrowStyles);

export { invertedArrowStyles, useInvertedArrowStyles };

export { default } from './invertedArrow.styles';
