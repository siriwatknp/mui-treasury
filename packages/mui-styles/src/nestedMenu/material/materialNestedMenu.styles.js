import actionToggleStyles from '../../menuItem/material/actionToggleItem.styles';
import infoItemStyles from '../../menuItem/material/infoMenuItem.styles';

export default theme => {
  const action = actionToggleStyles(theme);
  const info = infoItemStyles(theme);
  return {

  }
}

