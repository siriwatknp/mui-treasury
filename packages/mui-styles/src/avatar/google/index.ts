import makeStyles from '@material-ui/core/styles/makeStyles';
import googleAvatarStyles from './googleAvatar.styles';

const useGoogleAvatarStyles = makeStyles(googleAvatarStyles, { name: "GoogleAvatar" });

export { googleAvatarStyles, useGoogleAvatarStyles };

export { default } from './googleAvatar.styles';
