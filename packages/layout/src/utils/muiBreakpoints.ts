import createBreakpoints from "@material-ui/core/styles/createBreakpoints";

const MuiBreakpoints = createBreakpoints({});

// todo: wait for v5 to fix
export const BREAKPOINT_KEYS = MuiBreakpoints.keys;
