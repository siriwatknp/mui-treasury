import React from 'react';
import Button from '@material-ui/core/Button';

const PeaButton = props => <Button {...props} />;

PeaButton.metadata = {
  name: 'Pea Button',
};
PeaButton.codeSandbox = 'https://codesandbox.io/s/2vk8l2j2wn';

export default PeaButton;
